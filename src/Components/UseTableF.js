import { Table } from '@material-ui/core'
import React from 'react'

export default function UseTableF(records,headCells) {
  const TblContainer= props => (
    <Table>

    </Table>
  ) 
  return {
    TblContainer
  }
}
 